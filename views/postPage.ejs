<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>ToolBox</title>
    <link rel="stylesheet" href="css/bootstrap.css">
    <link rel="icon" href="images/favicon.ico?=v">
    <link rel="stylesheet" href="css/styles.css">
</head>

<body class="d-flex h-100 text-center text-bg-dark">
<div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
    <header class="mb-5">
        <div>
            <nav class="nav nav-masthead justify-content-center">
                <a class="nav-link fw-bold py-1 px-0 <%= homeNavActive %>" href="/">Home</a>
                <a class="nav-link fw-bold py-1 px-0 <%= BMINavActive %>" href="/BMI">BMI</a>
                <a class="nav-link fw-bold py-1 px-0 <%= toDoListNavActive %>" href="/ToDoList">To Do List</a>
                <a class="nav-link fw-bold py-1 px-0 <%= weatherNavActive %>" href="/Weather">Weather</a>
                <a class="nav-link fw-bold py-1 px-0 <%= signUpNavActive %>" href="/Mailchimp">Mailchimp</a>
                <a class="nav-link fw-bold py-1 px-0 <%= blogNavActive %>" href="/blog">Blog</a>
            </nav>
        </div>
    </header>
    <div class="postsText">
        <% posts.forEach(function (post) { %>
            <h1> <%= post.title %></h1>
            <p> <%= post.content.substring(0, 100) + " ..." %> <a href="/posts/<%= post.title %>">Read More</a></p>
        <% }) %>
    </div>
    <%- include("partials/footer"); -%>
</div>

<script src="js/bootstrap.esm.js"></script>
<script src="js/bootstrap.bundle.js"></script>
<script src="js/bootstrap.js"></script>
</body>


</html>